{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "baseUrl": ".", // This must be specified if "paths" is.
    "strict": true,
    "noImplicitAny": false,
    "paths": {
      "@sb/*": ["app/*"]
  }
  }
}
